<!DOCTYPE html>
<html>
<head><% include partials/page/head.ejs %></head>
<body>
  <% include partials/page/header.ejs %>
  <% include partials/page/nav.ejs %>
  
  <% if (!user) { %>
	    <p><h4>Welcome to our App!</h4>
         Please <a href="/login">log in</a>.</p>
  <% } else { %>
    	<p>Hello, <%= user.username %>. View your <a href="/profile">profile</a>.</p>
  
        <div class="col-lg-7">
          <p>
            <form action"/home" method="get">
              <input type="text" name="searchText" />
              <button type="submit">Search</button>
            </form>
          </p>
            <table border="1" width="100%"> 
              <tr>
                <th width="25%">Number</th> 
                <th width="25%">Shrt_Desc</th> 
                <th width="25%">GmWt_Desc1</th> 
                <th width="25%">GmWt_Desc2</th> 
              </tr>
              <% rows.forEach(function(item){ %>
                    <tr>
                      <td><a href="/details/<%= item.NDB_No %>"><%= item.NDB_No %></a></td> 
                      <td><%= item.Shrt_Desc %></td> 
                      <td><%= item.GmWt_Desc1 %></td> 
                      <td><%= item.GmWt_Desc2 %></td> 
                    </tr>
              <% }); %>
            </table>
          <div>
            <p>
             <a href="/home?page=<%= pageFirst %>">First</a> 
              <% if( pagePrev !== null) { %>
                 <a href="/home?page=<%= pagePrev %>">Prev</a>
              <% } %>
              
              <a class="active" href="/home?page=<%= curpg %>"><%= page %></a>
                
              <% if( pageTwo !== null) { %>
                <a href="/home?page=<%= pageTwo %>"><%= page + 1 %></a>
              <% } %>
              
              <% if( pageThree !== null) { %>
                <a href="/home?page=<%= pageThree %>"><%= page + 2 %></a>
              <% } %>  
              
              <% if( pageFour !== null) { %>    
                <a href="/home?page=<%= pageFour %>"><%= page + 3 %></a>
              <% } %>
              
              <% if( pageNext !== null ) { %> 
                 <a href="/home?page=<%= pageNext %>">Next</a> 
              <% } %> 
              
              <a href="/home?page=<%= pageLast %>">Last</a>
            </p>
          </div>
      
    <% } %>
  
  <% include partials/page/footer.ejs %>
  <% include partials/page/jsdefaults.ejs %>
</body>
</html>